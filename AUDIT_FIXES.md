# Agent Garden 修复与验收

本轮变更限于 Agent Garden 自动化、测试、文档与已批准的运行配置。Dream 与自建 Skill 晋升链路已退役，Skills 使用 Hermes 原生目录、审批和变更账本；OpenViking 与 Obsidian 保留。

## 实现状态

- [x] R01 异步记忆提取：通过公开 task/session 与归档标记区分受理、完成和失败。当前现场为 94 个完成归档，无未恢复失败或积压；保留原始会话。
- [x] R02 健康检查：校验任务、会话、归档的结构与必需字段，检测枚举上限；与同步器共用 manifest 校验规则，待清理记录也会使健康检查失败。
- [x] R07 同步恢复：请求发出前持久化上传意图，保留任务身份、精确清理来源和逐目标删除状态。普通失败在原上传终态确认后可重试；响应不明或语义失败保留人工恢复状态，空变更计划不会清除它们。
- [x] R10 调度：tick 仅运行 Garden 同步与默认 Hermes cron，两个阶段均有界超时并相互隔离。
- [x] R11 文件范围：上传拒绝符号链接和路径逃逸；清理验证精确来源与当前占用，保护重新创建的路径及祖先/后代资源。中文远端子资源按校验后的原始 URI 读取。
- [x] R13 保留边界：已退役的实现及入口移出运行链路。原始会话、知识、快照、历史日志、凭证、未知用途残留和 `.diagnostics/` 保留；额外清理需要独立证据与授权。
- [x] R14 最终独立复审：已报告的阻塞项已修复并通过回归与独立收尾复核；父会话重新核对代码指纹及全套测试，确认交付代码与通过复核的版本一致。

## 已执行验证

2026-09-15 父会话重新执行：

- Conda Python 3.11 全套 `unittest discover -s tests`：139 项通过。
- 五个原始清理反例先出现预期断言失败，修复后通过；新增测试覆盖删除响应与语义失败、崩溃恢复、来源重占、链式/反向移动、路径范围与内容竞态。
- 4 个 Garden Wiki 资源逐项真实回读：本地 SHA-256 与 manifest 一致，远端规范化正文一致。
- 真实语义检索命中架构概览，包含 OpenViking、Obsidian、原生 Skills 与后台维护关闭的现行说明。
- `healthcheck.py`：`semantic_healthy=true`，无任务、归档或同步枚举错误。
- 补充反例验证：上传超时或中断后仍保留请求前意图；会话枚举达到 1000 条上限时报错；manifest 拒绝非 64 位 ASCII 十六进制 SHA-256。
- 未知上传的重试在快照副作用前挂起，保留原恢复记录；新增断言覆盖快照禁写与 `pending.json` 字节不变。
- AnyRouter Astra 独立收尾复核（`deleg_816ed38c`）通过：136 项任务范围内测试和 4 项独立临时场景通过，安全与逻辑阻塞列表为空。该轮聚焦快照前置检查与上传重试；父会话全套 137 项还包括 1 项既有、非本任务的 provider 测试。
- 本机 Codex CLI 提交前复审发现并推动修复了任务归并遗漏与非主 profile 的 Micu 直连/备用路由约束；修复后复审通过，阻塞列表为空，139 项全套测试通过。
- Hermes 原生配置：创建提醒为 0，写入审批与账本启用，后台审查与 Curator 关闭。
- `git diff --check` 通过；Hermes 源码仓库 `git status --porcelain` 为空。

## 验证范围与恢复边界

删除、重命名及故障注入仅在临时目录、内存资源和模拟 HTTP 中执行，未对真实知识库执行破坏性验收。真实环境验证覆盖只读回读、语义检索、健康检查与无变更同步。

`cleanup_attempts` 表示已尝试但未完成核验的删除。同步器保留原操作信息并返回失败，等待人工核对公开接口与快照；自动重试不会把 404 当作语义修复证明。旧记录缺乏 `cleanup_sources` 时同样保留原目标等待核验。

同步、锁与原子状态仍由既有自动化实现，不新增调度器或平行晋升系统。真实长期知识质量留待正常使用样本评估，不以测试替身代替。
